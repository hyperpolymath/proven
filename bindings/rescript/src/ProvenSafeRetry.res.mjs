// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Safe_retryJs from "../../javascript/src/safe_retry.js";

function strategyToString(strategy) {
  switch (strategy) {
    case "Fixed" :
        return "fixed";
    case "Linear" :
        return "linear";
    case "Exponential" :
        return "exponential";
    
  }
}

function calculateDelay(prim0, prim1) {
  return Safe_retryJs.calculateDelay(prim0, prim1);
}

function retry(prim0, prim1) {
  return Safe_retryJs.retry(prim0, prim1);
}

var SafeRetryJs = {
  calculateDelay: calculateDelay,
  retry: retry
};

function presetQuick() {
  return {
          maxAttempts: 3,
          strategy: "fixed",
          baseDelay: 100,
          maxDelay: undefined,
          jitterFactor: 0.0,
          multiplier: undefined
        };
}

function presetStandard() {
  return {
          maxAttempts: 5,
          strategy: "exponential",
          baseDelay: 1000,
          maxDelay: 30000,
          jitterFactor: 0.1,
          multiplier: 2.0
        };
}

function presetAggressive() {
  return {
          maxAttempts: 10,
          strategy: "exponential",
          baseDelay: 500,
          maxDelay: 60000,
          jitterFactor: 0.2,
          multiplier: 2.0
        };
}

function presetGentle() {
  return {
          maxAttempts: 5,
          strategy: "linear",
          baseDelay: 5000,
          maxDelay: 60000,
          jitterFactor: 0.1,
          multiplier: 1.0
        };
}

export {
  strategyToString ,
  SafeRetryJs ,
  calculateDelay ,
  retry ,
  presetQuick ,
  presetStandard ,
  presetAggressive ,
  presetGentle ,
}
/* ../../javascript/src/safe_retry.js Not a pure module */
