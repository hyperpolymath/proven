// Generated by ReScript, PLEASE EDIT WITH CARE


function lengthToMeters(value, unit) {
  switch (unit) {
    case "Meters" :
        return value;
    case "Kilometers" :
        return value * 1000.0;
    case "Centimeters" :
        return value / 100.0;
    case "Millimeters" :
        return value / 1000.0;
    case "Miles" :
        return value * 1609.344;
    case "Yards" :
        return value * 0.9144;
    case "Feet" :
        return value * 0.3048;
    case "Inches" :
        return value * 0.0254;
    
  }
}

function lengthFromMeters(meters, unit) {
  switch (unit) {
    case "Meters" :
        return meters;
    case "Kilometers" :
        return meters / 1000.0;
    case "Centimeters" :
        return meters * 100.0;
    case "Millimeters" :
        return meters * 1000.0;
    case "Miles" :
        return meters / 1609.344;
    case "Yards" :
        return meters / 0.9144;
    case "Feet" :
        return meters / 0.3048;
    case "Inches" :
        return meters / 0.0254;
    
  }
}

function convertLength(value, from, to_) {
  var meters = lengthToMeters(value, from);
  return lengthFromMeters(meters, to_);
}

function massToKilograms(value, unit) {
  switch (unit) {
    case "Kilograms" :
        return value;
    case "Grams" :
        return value / 1000.0;
    case "Milligrams" :
        return value / 1000000.0;
    case "Pounds" :
        return value * 0.453592;
    case "Ounces" :
        return value * 0.0283495;
    case "Stones" :
        return value * 6.35029;
    
  }
}

function massFromKilograms(kg, unit) {
  switch (unit) {
    case "Kilograms" :
        return kg;
    case "Grams" :
        return kg * 1000.0;
    case "Milligrams" :
        return kg * 1000000.0;
    case "Pounds" :
        return kg / 0.453592;
    case "Ounces" :
        return kg / 0.0283495;
    case "Stones" :
        return kg / 6.35029;
    
  }
}

function convertMass(value, from, to_) {
  var kg = massToKilograms(value, from);
  return massFromKilograms(kg, to_);
}

function convertTemperature(value, from, to_) {
  var kelvin;
  switch (from) {
    case "Celsius" :
        kelvin = value + 273.15;
        break;
    case "Fahrenheit" :
        kelvin = (value - 32.0) * 5.0 / 9.0 + 273.15;
        break;
    case "Kelvin" :
        kelvin = value;
        break;
    
  }
  switch (to_) {
    case "Celsius" :
        return kelvin - 273.15;
    case "Fahrenheit" :
        return (kelvin - 273.15) * 9.0 / 5.0 + 32.0;
    case "Kelvin" :
        return kelvin;
    
  }
}

function timeToSeconds(value, unit) {
  switch (unit) {
    case "Seconds" :
        return value;
    case "Milliseconds" :
        return value / 1000.0;
    case "Microseconds" :
        return value / 1000000.0;
    case "Nanoseconds" :
        return value / 1000000000.0;
    case "Minutes" :
        return value * 60.0;
    case "Hours" :
        return value * 3600.0;
    case "Days" :
        return value * 86400.0;
    case "Weeks" :
        return value * 604800.0;
    
  }
}

function timeFromSeconds(secs, unit) {
  switch (unit) {
    case "Seconds" :
        return secs;
    case "Milliseconds" :
        return secs * 1000.0;
    case "Microseconds" :
        return secs * 1000000.0;
    case "Nanoseconds" :
        return secs * 1000000000.0;
    case "Minutes" :
        return secs / 60.0;
    case "Hours" :
        return secs / 3600.0;
    case "Days" :
        return secs / 86400.0;
    case "Weeks" :
        return secs / 604800.0;
    
  }
}

function convertTime(value, from, to_) {
  var secs = timeToSeconds(value, from);
  return timeFromSeconds(secs, to_);
}

function dataToBytes(value, unit) {
  switch (unit) {
    case "Bytes" :
        return value;
    case "Kilobytes" :
        return value * 1000.0;
    case "Megabytes" :
        return value * 1000000.0;
    case "Gigabytes" :
        return value * 1000000000.0;
    case "Terabytes" :
        return value * 1000000000000.0;
    case "Kibibytes" :
        return value * 1024.0;
    case "Mebibytes" :
        return value * 1048576.0;
    case "Gibibytes" :
        return value * 1073741824.0;
    case "Tebibytes" :
        return value * 1099511627776.0;
    
  }
}

function dataFromBytes(bytes, unit) {
  switch (unit) {
    case "Bytes" :
        return bytes;
    case "Kilobytes" :
        return bytes / 1000.0;
    case "Megabytes" :
        return bytes / 1000000.0;
    case "Gigabytes" :
        return bytes / 1000000000.0;
    case "Terabytes" :
        return bytes / 1000000000000.0;
    case "Kibibytes" :
        return bytes / 1024.0;
    case "Mebibytes" :
        return bytes / 1048576.0;
    case "Gibibytes" :
        return bytes / 1073741824.0;
    case "Tebibytes" :
        return bytes / 1099511627776.0;
    
  }
}

function convertData(value, from, to_) {
  var bytes = dataToBytes(value, from);
  return dataFromBytes(bytes, to_);
}

function formatBytesDecimal(bytes) {
  if (bytes < 1000.0) {
    return bytes.toFixed(0) + " B";
  } else if (bytes < 1000000.0) {
    return (bytes / 1000.0).toFixed(2) + " KB";
  } else if (bytes < 1000000000.0) {
    return (bytes / 1000000.0).toFixed(2) + " MB";
  } else if (bytes < 1000000000000.0) {
    return (bytes / 1000000000.0).toFixed(2) + " GB";
  } else {
    return (bytes / 1000000000000.0).toFixed(2) + " TB";
  }
}

function formatBytesBinary(bytes) {
  if (bytes < 1024.0) {
    return bytes.toFixed(0) + " B";
  } else if (bytes < 1048576.0) {
    return (bytes / 1024.0).toFixed(2) + " KiB";
  } else if (bytes < 1073741824.0) {
    return (bytes / 1048576.0).toFixed(2) + " MiB";
  } else if (bytes < 1099511627776.0) {
    return (bytes / 1073741824.0).toFixed(2) + " GiB";
  } else {
    return (bytes / 1099511627776.0).toFixed(2) + " TiB";
  }
}

function formatDuration(seconds) {
  if (seconds < 0.001) {
    return (seconds * 1000000.0).toFixed(0) + " us";
  }
  if (seconds < 1.0) {
    return (seconds * 1000.0).toFixed(2) + " ms";
  }
  if (seconds < 60.0) {
    return seconds.toFixed(2) + " s";
  }
  if (seconds < 3600.0) {
    var mins = Math.floor(seconds / 60.0);
    var secs = seconds % 60.0;
    return mins.toFixed(0) + "m " + secs.toFixed(0) + "s";
  }
  if (seconds < 86400.0) {
    var hours = Math.floor(seconds / 3600.0);
    var mins$1 = Math.floor(seconds % 3600.0 / 60.0);
    return hours.toFixed(0) + "h " + mins$1.toFixed(0) + "m";
  }
  var days = Math.floor(seconds / 86400.0);
  var hours$1 = Math.floor(seconds % 86400.0 / 3600.0);
  return days.toFixed(0) + "d " + hours$1.toFixed(0) + "h";
}

export {
  lengthToMeters ,
  lengthFromMeters ,
  convertLength ,
  massToKilograms ,
  massFromKilograms ,
  convertMass ,
  convertTemperature ,
  timeToSeconds ,
  timeFromSeconds ,
  convertTime ,
  dataToBytes ,
  dataFromBytes ,
  convertData ,
  formatBytesDecimal ,
  formatBytesBinary ,
  formatDuration ,
}
/* No side effect */
